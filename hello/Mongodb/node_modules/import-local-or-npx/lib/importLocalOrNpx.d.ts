import { Imported } from './types.js';

interface ImportLocalOrNpxSettings {
    importer?: ModuleImporter;
    logger?: NpxImportLogger;
}
type ModuleImporter = (source: string) => Promise<object>;
type NpxImportLogger = (message: string) => void;
declare function importLocalOrNpx(specifier: string, { importer, logger, }?: ImportLocalOrNpxSettings): Promise<Imported>;

export { type ImportLocalOrNpxSettings, type ModuleImporter, type NpxImportLogger, importLocalOrNpx };
