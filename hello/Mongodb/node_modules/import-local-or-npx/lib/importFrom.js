import enhancedResolve from "enhanced-resolve";
async function importFrom(directory, packageName, importer) {
  const main = await new Promise((resolve) => {
    enhancedResolve(directory, packageName, (error, result) => {
      resolve(error ?? result);
    });
  });
  try {
    return typeof main === "string" ? await importer(main) : main;
  } catch (error) {
    return error;
  }
}
export {
  importFrom
};
//# sourceMappingURL=importFrom.js.map