export function assertNoDuplicateBlocks(presetDefinition) {
    const presetName = getName(presetDefinition);
    const seen = new Set();
    for (const block of presetDefinition.blocks) {
        if (seen.has(block)) {
            throw new Error(`Preset ${presetName} has duplicate Block: ${getName(block)}`);
        }
    }
}
function getName(value) {
    return value.about?.name ?? "(anonymous)";
}
//# sourceMappingURL=assertNoDuplicateBlocks.js.map