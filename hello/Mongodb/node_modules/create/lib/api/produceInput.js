import { createNativeSystems } from "../system/createNativeSystems.js";
export function produceInput(input, settings) {
    const { system, take } = createNativeSystems(settings);
    return input({
        args: settings.args,
        take,
        ...system,
    });
}
//# sourceMappingURL=produceInput.js.map