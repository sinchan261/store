export async function clearLocalGitTags(runner) {
    const tags = await runner("git tag -l");
    if (tags.stdout) {
        await runner(`git tag -d ${tags.stdout.toString().replaceAll("\n", " ")}`);
    }
}
//# sourceMappingURL=clearLocalGitTags.js.map