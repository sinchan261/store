import * as fs from "node:fs";
export function validateNewDirectory(value) {
    if (value && fs.existsSync(value)) {
        return `That directory already exists. Please choose another one.`;
    }
    return validateText(value);
}
export function validateNumber(value) {
    if (isNaN(parseFloat(value))) {
        return "Please enter a numeric value.";
    }
}
export function validatorFromSchema(schema) {
    return (value) => {
        return (schema.safeParse(value).error?.errors[0].message ?? validateText(value));
    };
}
function validateText(value) {
    if (!value) {
        return "Please enter a value.";
    }
}
//# sourceMappingURL=validators.js.map