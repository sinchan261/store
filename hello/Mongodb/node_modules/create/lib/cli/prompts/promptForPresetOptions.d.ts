import { AnyShape, InferredObject } from "../../options.js";
import { Base } from "../../types/bases.js";
import { SystemContext } from "../../types/system.js";
export interface PromptForPresetOptionsSettings {
    base: Base;
    existingOptions: Partial<InferredObject<AnyShape>>;
    system: SystemContext;
}
export declare function promptForPresetOptions({ base, existingOptions, system, }: PromptForPresetOptionsSettings): Promise<symbol | {
    [x: string]: any;
}>;
