import { parseArgs } from "node:util";
export function parseArgsPreset(args) {
    const { values } = parseArgs({
        args,
        options: {
            preset: {
                type: "string",
            },
        },
        strict: false,
    });
    return values.preset;
}
//# sourceMappingURL=parseArgvPreset.js.map