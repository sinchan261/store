export function assertOptionsForInitialize(options) {
    for (const key of ["owner", "repository"]) {
        if (typeof options[key] !== "string") {
            throw new Error(`To run with --mode initialize, the Template must have a --${key} Option of type string.`);
        }
    }
}
//# sourceMappingURL=assertOptionsForInitialize.js.map