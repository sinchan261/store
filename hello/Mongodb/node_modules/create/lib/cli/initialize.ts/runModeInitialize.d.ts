export interface RunModeInitializeSettings {
    args: string[];
    directory?: string;
    from?: string;
    preset?: string;
}
export declare function runModeInitialize({ args, directory: requestedDirectory, from, preset: requestedPreset, }: RunModeInitializeSettings): Promise<{
    outro: string;
    status: 1;
} | {
    status: 2;
    outro?: undefined;
}>;
