export function isLocalPath(from) {
    return [".", "/", "~"].includes(from[0]);
}
export function isTemplate(value) {
    return (!!value &&
        typeof value === "object" &&
        "presets" in value &&
        typeof value.presets === "object");
}
//# sourceMappingURL=utils.js.map