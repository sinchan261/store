import { ProductionMode } from "../modes/types.js";
import { ProductionSettings } from "./types.js";
export interface ReadProductionSettingsOptions {
    directory?: string;
    mode?: ProductionMode;
}
export declare function readProductionSettings({ directory, mode, }?: ReadProductionSettingsOptions): Promise<Error | ProductionSettings>;
