import * as prompts from "@clack/prompts";
import { validateBlankDirectory } from "./validators.js";
export async function promptForDirectory(requested, template) {
    if (requested) {
        // TODO: Attempt to create the folder on disk?
        return requested;
    }
    return await prompts.text({
        initialValue: template.about?.name &&
            `my-${template.about.name.toLowerCase().replaceAll(" ", "-")}`,
        message: "What directory would you like to create the repository in?",
        validate: validateBlankDirectory,
    });
}
//# sourceMappingURL=promptForDirectory.js.map