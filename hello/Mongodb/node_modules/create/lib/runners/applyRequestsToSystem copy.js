export async function applyRequestsToSystem(requests, system) {
    await Promise.all(requests.map(async (request) => {
        try {
            await request.send(system.fetchers);
        }
        catch (error) {
            system.display.error({
                error,
                message: `Error sending ${request.id}:`,
            });
        }
    }));
}
//# sourceMappingURL=applyRequestsToSystem%20copy.js.map