import { Block, BlockWithAddons } from "../types/blocks.js";
import { CreatedBlockAddons, Creation } from "../types/creations.js";
export interface BlockProduction<Addons extends object | undefined, Options extends object> {
    addons: Addons;
    creation?: Partial<Creation<Options>>;
}
export declare function getUpdatedBlockAddons<Options extends object>(blockProductions: Map<Block<object | undefined, Options>, BlockProduction<object, Options>>, newBlockAddons?: CreatedBlockAddons<object, Options>[]): [BlockWithAddons<object, Options>, object][];
