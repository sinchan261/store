export function mergeRequests(firsts, seconds) {
    const byId = new Map();
    for (const request of [...firsts, ...seconds]) {
        byId.set(request.id, request);
    }
    return Array.from(byId.values());
}
//# sourceMappingURL=mergeRequests.js.map