export function applyMerger(first, second, merger, fallback) {
    if (!first || !second) {
        return second ?? first ?? fallback;
    }
    return merger(first, second);
}
//# sourceMappingURL=applyMergerWithDefault.js.map