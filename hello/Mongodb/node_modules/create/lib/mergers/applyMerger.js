export function applyMerger(first, second, merger) {
    if (first == null || second == null) {
        return second ?? first ?? undefined;
    }
    return merger(first, second);
}
//# sourceMappingURL=applyMerger.js.map