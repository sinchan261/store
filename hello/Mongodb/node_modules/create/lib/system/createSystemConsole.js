export function createSystemConsole() {
    return {
        error: (error) => {
            if (error instanceof Error || typeof error === "string") {
                console.error(error);
            }
            else {
                console.error(error.message, error.error);
            }
        },
        log: (message) => {
            console.log(message);
        },
    };
}
//# sourceMappingURL=createSystemConsole.js.map