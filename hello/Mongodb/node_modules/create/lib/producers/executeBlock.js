import { mergeCreations } from "../mergers/mergeCreations.js";
export function executeBlock(block, blockContext) {
    let creation = block.produce(blockContext);
    // From engine/runtime/execution.md:
    // 2.2. If a mode is specified, additionally generate the appropriate Block Creations
    if (blockContext.mode === "new" && block.initialize) {
        creation = mergeCreations(creation, block.initialize(blockContext));
    }
    return creation;
}
//# sourceMappingURL=executeBlock.js.map