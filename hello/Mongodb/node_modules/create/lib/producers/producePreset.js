import { createSystemContextWithAuth } from "../system/createSystemContextWithAuth.js";
import { executePresetBlocks } from "./executePresetBlocks.js";
export async function producePreset(preset, { directory = ".", mode, options, ...providedSystem }) {
    const system = await createSystemContextWithAuth({
        directory,
        ...providedSystem,
    });
    const creation = executePresetBlocks(preset, options, { ...system, directory }, mode);
    return {
        addons: [],
        files: {},
        requests: [],
        scripts: [],
        suggestions: [],
        ...creation,
    };
}
//# sourceMappingURL=producePreset.js.map